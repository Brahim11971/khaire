Canela Text is bundled and wired up — nothing to do.

Bundled weights (all as .woff2, ~21 KB each):
    CanelaText-Light.woff2      (weight 300)
    CanelaText-Regular.woff2    (weight 400)
    CanelaText-Medium.woff2     (weight 500, default display)
    CanelaText-Bold.woff2       (weight 700)

Note: these are *trial* files from Commercial Type. Buy a commercial
license at https://commercialtype.com/catalog/canela before launching
to the public.

To add more weights (italics, Thin, Black), drop the file here and
add a matching @font-face block in /css/style.css.
